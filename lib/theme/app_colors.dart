import 'package:flutter/material.dart';

class AppColors {
  // Primary & Accent Colors
  static const Color primary = Color(0xFF00828A); // Main teal color used in logo and buttons
  static const Color primaryDark = Color(0xFF006B72);
  static const Color primaryLight = Color(0xFFE5F2F3); // Light teal for backgrounds
  
  // Background Colors
  static const Color background = Color(0xFFF2F6F8); // Very light blue/grey background
  static const Color surface = Colors.white;
  static const Color surfaceHighlighted = Color(0xFFF8FAFC);

  // Text Colors
  static const Color textPrimary = Color(0xFF1E293B); // Dark slate
  static const Color textSecondary = Color(0xFF64748B); // Medium slate
  static const Color textLight = Color(0xFF94A3B8); // Light slate

  // Status/Feedback Colors
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color errorLight = Color(0xFFFEE2E2);

  // Borders and Dividers
  static const Color border = Color(0xFFE2E8F0);
  
  // Navigation
  static const Color navInactive = Color(0xFF94A3B8);
  static const Color navActive = Color(0xFF0F172A); // Dark navy for active nav
  static const Color navActiveBg = Color(0xFF0F172A); // Dark navy bg for active nav item
}
